apiVersion: v1
kind: Service
metadata:
  name: {{ include "acct.fullname" . }}
  labels:
    {{- include "acct.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "acct.selectorLabels" . | nindent 4 }}
