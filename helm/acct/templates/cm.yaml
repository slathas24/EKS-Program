apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "acct.fullname" . }}-config
  labels:
    {{- include "acct.labels" . | nindent 4 }}
data:
    DB_DET: {{ .Values.cm.dbDet | quote }}
    DB_PORT: {{ .Values.cm.dbPort | quote }}
